let myName: string;
let meaningOfLife: number;
let isLoading: boolean;
let defeatsPurposeOfTypescript: any;
let unionType: string | number;

myName = "nik";
meaningOfLife = 0;
isLoading = false;
defeatsPurposeOfTypescript = 1;
defeatsPurposeOfTypescript = "might be useful sometimes";

const sum = (a: number, b: string) => {
    return a + b;
};

unionType = 2026;
unionType = "Jan";

let postId: string | number;
let isActive: number | boolean;

let re: RegExp = /\w+/g;
